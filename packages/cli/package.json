{
  "name": "keytrace",
  "version": "0.1.0",
  "description": "CLI for keytrace identity verification — create and self-sign identity claims on ATProto",
  "type": "module",
  "bin": {
    "kt": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint not configured'"
  },
  "dependencies": {
    "@atproto/api": "^0.14.0",
    "@clack/prompts": "^0.9.0",
    "@keytrace/runner": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/orta/keytrace",
    "directory": "packages/cli"
  },
  "engines": {
    "node": ">=20"
  }
}
